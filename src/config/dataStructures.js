// src/config/dataStructures.js
/**
 * Configuration constants for wave count display
 */

export const waveCountConfig = {
  // Default selection when component loads
  defaultScenarioId: "primary",

  // UI display options
  displayOptions: {
    showProbability: true,
    showMetrics: true,
    showVerdict: true,
  },
};
